# Server-only placeholders. Configure real values manually in Vercel; never
# prefix these with NEXT_PUBLIC_ or commit populated values.
RESEND_API_KEY=
RESEND_FROM_EMAIL=
CONTACT_TO_EMAIL=
CONTACT_FORM_ENABLED=false
